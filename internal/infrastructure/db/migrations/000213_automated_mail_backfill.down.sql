-- Nothing to undo safely. A renamed label may have adopted one a person made,
-- so renaming back could change their label; the previous release recreates
-- its own names on its next follow-up sweep.
SELECT 1;
