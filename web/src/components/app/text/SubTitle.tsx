export default function SubTitle({ children }: { children: React.ReactNode }) {
    return <p className='text-gray-500 text-[14px] font-inter mb-2 whitespace-[initial]'>
        {children}
    </p>
}
