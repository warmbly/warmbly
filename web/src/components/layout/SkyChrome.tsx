// Background chrome. Kept as a named component so the shell import path
// stays stable, but in the Linear-style theme there's nothing to paint —
// the shell is white-on-white with hairline dividers, no atmospheric
// background. If we ever bring decoration back, this is where it lands.

export function SkyChrome() {
    return null;
}
