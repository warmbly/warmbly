{
    "url": "/contacts/lookup",
    "method": "GET",
    "qs": {
        "email": "{{parameters.email}}"
    },
    "response": {
        "iterate": "{{if(body.contact, add(emptyarray, body.contact), emptyarray)}}",
        "output": "{{item}}"
    }
}
