{
    "url": "/campaigns/{{parameters.campaign_id}}/stop",
    "method": "POST",
    "response": {
        "output": {
            "campaign_id": "{{parameters.campaign_id}}",
            "status": "{{body.status}}"
        }
    }
}
