{
    "url": "/folders",
    "method": "POST",
    "body": {
        "title": "{{parameters.title}}",
        "color": "{{parameters.color}}"
    },
    "response": {
        "output": "{{body}}"
    }
}
