{
    "url": "/campaigns",
    "method": "POST",
    "body": {
        "name": "{{parameters.name}}",
        "description": "{{parameters.description}}",
        "daily_limit": "{{parameters.daily_limit}}"
    },
    "response": {
        "output": "{{body}}"
    }
}
