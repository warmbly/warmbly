{
    "url": "/contacts/{{parameters.contact_id}}/notes/{{parameters.note_id}}",
    "method": "PATCH",
    "body": {
        "content": "{{parameters.content}}"
    },
    "response": {
        "output": "{{body}}"
    }
}
