{
    "url": "/analytics/dashboard",
    "method": "GET",
    "qs": {
        "period": "{{parameters.period}}"
    },
    "response": {
        "iterate": "{{if(body, add(emptyarray, body), emptyarray)}}",
        "output": "{{item}}"
    }
}
