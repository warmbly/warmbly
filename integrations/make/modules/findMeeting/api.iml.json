{
    "url": "/meetings",
    "method": "GET",
    "qs": {
        "q": "{{parameters.query}}",
        "limit": 25
    },
    "response": {
        "iterate": "{{body.data}}",
        "output": "{{item}}"
    }
}
